# ─────────────────────────────────────────────────────────────────
# Edge TTS API — Python dependencies
# Target: Python 3.11.9 (set PYTHON_VERSION=3.11.9 in Render dashboard)
# All packages are pure-Python or have Python 3.11 pre-built wheels
# ─────────────────────────────────────────────────────────────────

# Web framework — pure Python wheels available for 3.11
fastapi==0.111.0
uvicorn[standard]==0.29.0
starlette==0.37.2

# TTS engine — v7.2.8 has the 403/Sec-MS-GEC fix
edge-tts==7.2.8

# PDF text extraction — pure Python
PyPDF2==3.0.1

# Pydantic — explicitly pinned so pip uses the 3.11 pre-built wheel
# and never falls back to compiling pydantic-core from Rust
pydantic==2.7.1
pydantic-core==2.18.4

# Async HTTP — required by edge-tts v7
aiohttp==3.10.11
aiosignal==1.3.1
frozenlist==1.4.1
multidict==6.0.5
yarl==1.9.4

# Multipart file upload
python-multipart==0.0.9

# Uvicorn extras (already covered by [standard] but explicit = safer)
httptools==0.6.1
uvloop==0.19.0
websockets==12.0