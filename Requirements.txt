# ─────────────────────────────────────────────────────────────────
# Edge TTS API — Python dependencies
# Tested on Python 3.11 (Render free tier default)
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# TTS engine (Microsoft Edge neural voices — no API key needed)
edge-tts==6.1.12

# PDF text extraction
PyPDF2==3.0.1

# Data validation (used internally by FastAPI)
pydantic==2.7.1

# Async HTTP (required by edge-tts)
aiohttp==3.9.5

# Multipart form / file upload support
python-multipart==0.0.9